# 邮箱域名配置（多域名以逗号分隔）
VITE_EMAIL_DOMAIN=example.com,abc.com,def.com

# API地址配置
VITE_API_BASE_URL=https://api.example.com

# 本地 `pnpm dev` 时 /api 代理目标（默认演示站，可改为自托管 Worker 地址）
# VITE_DEV_PROXY_TARGET=https://zmailr.itool.eu.cc

# 其他配置
VITE_API_URL=/api 